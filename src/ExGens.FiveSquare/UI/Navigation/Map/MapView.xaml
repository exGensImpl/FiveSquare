<UserControl x:Class="ExGens.FiveSquare.UI.Navigation.Map.MapView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ExGens.FiveSquare.UI.Navigation.Map"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance local:MapViewModel}"
             DataContext="{DynamicResource VM}">
    <DockPanel>
      <TextBlock DockPanel.Dock="Top" Text="Map" FontSize="24"/>
      <local:BindableMap x:Name="Map" LayerSource="{Binding Layers}" Location="{Binding Location, UpdateSourceTrigger=PropertyChanged}"/> 
    </DockPanel>
</UserControl>
